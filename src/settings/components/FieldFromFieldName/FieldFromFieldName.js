import {
  FieldSelectionFinal,
} from '@folio/stripes-acq-components';

export const FieldFromFieldName = ({
  
}) => {
  return (
    <FieldSelectionFinal
      name="name"
      id="id"
      dataOptions={[]}
    />
  );
};
