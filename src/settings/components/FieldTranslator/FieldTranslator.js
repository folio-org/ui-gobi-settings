import {
  FieldSelectionFinal,
} from '@folio/stripes-acq-components';

export const FieldTranslator = ({

}) => {
  return (
    <FieldSelectionFinal
      name="name"
      id="id"
      dataOptions={[]}
    />
  );
};
